<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema base garantizado -->
    <style name="Theme.CharmeetChic" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- colores opcionales -->
    </style>

    <!-- Tema del SplashScreen -->
    <style name="Theme.CharmeetChic.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/charme_logo</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="postSplashScreenTheme">@style/Theme.CharmeetChic</item>
    </style>

</resources>
